// constants.dart

import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(
    0xFF1E3A8A,
  ); // Blue shade from the 'Jôbizz' text
  static const Color secondaryColor = Color(
    0xFF616161,
  ); // Grey color from Text widget
  static const Color backgroundColor = Color(
    0xFFF5F5F5,
  ); // Background grey color
  static const Color accentColor = Color(
    0xFF0D47A1,
  ); // Darker blue accent (for buttons or highlights)
  static const Color dividerColor = Color(0xFFBDBDBD); // Color for dividers
  static const Color textColor = Color(
    0xFF1976D2,
  ); // Text color for links (like 'Log in' text)
  static const Color whitecolor = Colors.white;
  static const Color blackcolor = Colors.black87;
  static const Color blueslider = Colors.blueAccent;
  static const Color buttoncolor = Color(0xFF1E3A8A);
}
