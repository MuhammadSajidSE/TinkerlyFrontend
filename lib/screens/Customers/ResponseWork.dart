import 'package:flutter/material.dart';

class CustomerResponsework extends StatelessWidget {
  const CustomerResponsework({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Customer Response Work"),
    );
  }
}
