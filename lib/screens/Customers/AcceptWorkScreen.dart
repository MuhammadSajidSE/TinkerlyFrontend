import 'package:flutter/material.dart';

class Acceptworkscreen extends StatelessWidget {
  const Acceptworkscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Accept Work Screen"),
    );
  }
}
